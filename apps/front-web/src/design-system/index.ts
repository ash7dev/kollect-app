// Design System Kollect — Barrel export
export {
    colors,
    gradients,
    typography,
    fontWeights,
    spacing,
    layout,
    radius,
    shadows,
    animations,
    theme,
} from './tokens';

export type {
    Theme,
    Colors,
    Typography,
    Spacing,
    Radius,
    Shadows,
    Animations,
} from './tokens';
